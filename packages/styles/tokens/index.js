/**
 * Design tokens index
 * Exports all design tokens for easy consumption
 */

const { colors, semanticColors } = require('./colors');
const { typography, semanticTypography } = require('./typography');
const { spacing, semanticSpacing, borderRadius, boxShadow } = require('./spacing');

module.exports = {
  colors,
  semanticColors,
  typography,
  semanticTypography,
  spacing,
  semanticSpacing,
  borderRadius,
  boxShadow,
};